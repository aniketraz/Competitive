#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    // Input string
    std::string S;
    std::cin >> S;

    // Print each character with a space
    for (int i = 0; i < S.length(); ++i) {
        std::cout << S[i] << " ";
    }

    return 0;
}